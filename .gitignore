node_modules
.next
.env
.env.local
.env*.local
.DS_Store
.vercel
tsconfig.tsbuildinfo
videoDirectory

# Video ingest pipeline (scripts/)
/uploads
/scripts/.ingest-state.json

# Local launch-video working files (large media, never commit)
/launch-video

# Claude Code transient files
.claude/scheduled_tasks.lock
.env*
